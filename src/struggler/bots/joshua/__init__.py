"""Joshua: the learned `Player` (see docs/WOPR.md).

`features` is the numpy-only encoding contract shared with the WOPR training
arena; `model`/`player` need torch (`pip install -e ".[joshua]"`).
"""
